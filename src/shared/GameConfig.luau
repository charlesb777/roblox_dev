-- GameConfig is a ModuleScript: shared settings both server and client can read.
-- Change a number here, save, and watch it update live in Studio.

local GameConfig = {
	GAME_NAME = "Roblox Cursor Starter",
	STARTING_COINS = 0,
	COIN_VALUE = 1,   -- try changing this to 5 and re-test!
}

return GameConfig
